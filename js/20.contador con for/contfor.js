/**
 * contador con ciclo for 
 * autor:JR
 * Fecha: 16 de abril del 2024
 * version:42
 */

function contar(pnumHasta){
    let numHasta=pnumHasta;
    let contador;
   
    for(contador=1; contador<=numHasta; contador++){
        console.log(contador);

    }

}

const contarExpresion=function(pnumHasta){
    let numHasta=pnumHasta;
    let contador;
   
    for(contador=1; contador<=numHasta; contador++){
        console.log(contador);

    }

}