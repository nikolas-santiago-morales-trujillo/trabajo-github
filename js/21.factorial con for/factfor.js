/**
 * factorial con ciclo for 
 * autor:JR
 * Fecha: 16 de abril del 2024
 * version:44
 */

function calcular(pnum){
    let num=pnum;
    let factorial=1;
    for(contador=1;contador<=num;contador++){
        factorial= factorial*contador;
       console.log (factorial);
    }
}
const calcularExpresion=function(pnum){
    let num=pnum;
    let factorial=1;
    for(contador=1;contador<=num;contador++){
        factorial= factorial*contador;
       console.log (factorial);
    }
}